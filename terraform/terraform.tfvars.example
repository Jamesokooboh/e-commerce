# Copy to terraform.tfvars (gitignored) and fill in your own values.

# Your public IP, /32 — find it with `curl -s https://checkip.amazonaws.com`.
# Never leave this as 0.0.0.0/0.
allowed_ssh_cidr = "203.0.113.10/32"
